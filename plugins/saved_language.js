export default defineNuxtPlugin(async (nuxtApp) => {

    // if (process.client) {
    //     const language = useState('language');

    //     const saved_language = localStorage.getItem('language_ext');
    //     if (saved_language) {
    //         console.log(saved_language)
    //         language.value = saved_language;
    //     }
    // }



})